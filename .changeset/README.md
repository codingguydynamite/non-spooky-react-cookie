# Changesets

This folder holds pending release notes. Every pull request that changes the
published package adds one file here via `pnpm changeset`. The release
workflow turns them into a version bump and a CHANGELOG entry.

See `MAINTAINING.md` for the full release flow.
