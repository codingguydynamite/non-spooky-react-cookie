---
"non-spooky-react-cookie": minor
---

`language` now accepts region codes and ignores case: `"pl-PL"`, `"de_AT"` and `"EN-us"` resolve to the built-in `pl`, `de` and `en` texts instead of falling back to English.
